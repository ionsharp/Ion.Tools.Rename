<Window x:Class="Ion.Tools.Rename.MainView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i="clr-namespace:Ion.Tools;assembly=Ion.Tools" 
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:local="clr-namespace:Ion.Tools.Rename"
    mc:Ignorable="d"
    MaxHeight="720"
    Title="Rename" 
    ResizeMode="NoResize"
    SizeToContent="Height"
    Width="540"
    WindowStartupLocation="CenterScreen"
    WindowStyle="SingleBorderWindow">
    <Grid
        Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TabControl Grid.Row="0">
            <TabItem Header="Options">
                <Grid Margin="8px">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <Label Grid.Row="0">Folder path</Label>
                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding FolderPath, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
                            <TextBox.Resources>
                                <Style TargetType="Border">
                                    <Setter Property="CornerRadius" Value="4"/>
                                </Style>
                            </TextBox.Resources>
                        </TextBox>
                        <i:Image Grid.Column="1"
                            Cursor="Hand" 
                            Foreground="#AAA"
                            Margin="8px 0 0 0"
                            MouseDown="onBrowse"
                            Source="pack://application:,,,/Ion.Tools.Rename;component/Image/Folder.png"
                            SourceSize="16,16"
                            ToolTip="Browse..."/>
                    </Grid>

                    <Label Grid.Row="2">File extension</Label>
                    <ComboBox Grid.Row="3" 
                        SelectedIndex="{Binding FileExtension, Mode=TwoWay}">
                        <ComboBoxItem>Original</ComboBoxItem>
                        <ComboBoxItem>Capitalized</ComboBoxItem>
                        <ComboBoxItem>Lower</ComboBoxItem>
                        <ComboBoxItem>Upper</ComboBoxItem>
                    </ComboBox>

                    <Label Grid.Row="4">File name increment base</Label>
                    <ComboBox Grid.Row="5" 
                        SelectedIndex="{Binding FileNameIncrementBase, Mode=TwoWay}">
                        <ComboBoxItem>Binary (2)</ComboBoxItem>
                        <ComboBoxItem>Decimal (10)</ComboBoxItem>
                        <ComboBoxItem>Hexadecimal (16)</ComboBoxItem>
                        <ComboBoxItem>Hexavigesimal (26)</ComboBoxItem>
                        <ComboBoxItem>Sexagesimal (60)</ComboBoxItem>
                    </ComboBox>

                    <Label Grid.Row="6">File name increment by</Label>
                    <ComboBox Grid.Row="7"
                        SelectedIndex="{Binding FileNameIncrementBy, Mode=TwoWay}">
                        <ComboBoxItem>All</ComboBoxItem>
                        <ComboBoxItem>Extension</ComboBoxItem>
                    </ComboBox>

                    <Label Grid.Row="8">File name increment start</Label>
                    <TextBox Grid.Row="9" x:Name="FileNameIncrementStart"
                        Text="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Label Grid.Row="10">File name increment</Label>
                    <TextBox Grid.Row="11" x:Name="FileNameIncrement"
                        Text="1">
                        <TextBox.Resources>
                            <Style TargetType="Border">
                                <Setter Property="CornerRadius" Value="4"/>
                            </Style>
                        </TextBox.Resources>
                    </TextBox>

                    <Label Grid.Row="12">File sort</Label>
                    <ComboBox Grid.Row="13"
                        SelectedIndex="{Binding FileSort, Mode=TwoWay}">
                        <ComboBoxItem>Accessed</ComboBoxItem>
                        <ComboBoxItem>Created</ComboBoxItem>
                        <ComboBoxItem>Extension</ComboBoxItem>
                        <ComboBoxItem>Modified</ComboBoxItem>
                        <ComboBoxItem>Name</ComboBoxItem>
                    </ComboBox>

                    <Label Grid.Row="14">File sort direction</Label>
                    <ComboBox Grid.Row="15"
                        SelectedIndex="{Binding FileSortDirection, Mode=TwoWay}">
                        <ComboBoxItem>Ascending</ComboBoxItem>
                        <ComboBoxItem>Descending</ComboBoxItem>
                    </ComboBox>
                </Grid>
            </TabItem>
            <TabItem Header="Preview">
                <ScrollViewer>
                    <DataGrid ItemsSource="{Binding Files}"
                        AutoGenerateColumns="False"
                        CanUserAddRows="False"
                        CanUserDeleteRows="False"
                        CanUserResizeColumns="True"
                        CanUserResizeRows="False"
                        CanUserReorderColumns="True"
                        CanUserSortColumns="False"
                        GridLinesVisibility="All"
                        HeadersVisibility="Column"
                        IsReadOnly="True">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="Old name"
                                Binding="{Binding OldName}"/>
                            <DataGridTextColumn Header="New name"
                                Binding="{Binding NewName}"/>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </TabItem>
        </TabControl>
        <Button Grid.Row="1" Content="Do"
            Click="onDo"
            Margin="0,8,0,0">
            <Button.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="4"/>
                </Style>
            </Button.Resources>
        </Button>
    </Grid>
</Window>